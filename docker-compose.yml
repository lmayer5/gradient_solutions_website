version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./private_data:/var/www/private_data
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html
      - ADMIN_PASSWORD=GolfTeeAdmin2025!
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=your_email@gmail.com
      - SMTP_PASS=your_app_password
      - SMTP_FROM_EMAIL=orders@martinitees.ca
      - SMTP_FROM_NAME=Martini Golf Tees Canada
